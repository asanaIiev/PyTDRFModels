name: Django CI/CD Pipeline

on:
  push:
    branches: [ "master" ]
#  pull_request:
#    branches: [ "master" ]

env:
  PYTHON_VERSION: '3.12'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Django Tests
        env:
          SECRET_KEY: "test-key"
          PYTHONPATH: ${{ github.workspace }}
        run: python settings/manage.py test app

#  deploy:
#    runs-on: ubuntu-latest
#    needs: [build, test]
#    steps:
#      - name: Deploy to Server via SSH
#        uses: appleboy/ssh-action@v1.0.3
#        with:
#          host: ${{ secrets.SERVER_HOST }}
#          username: ${{ secrets.SERVER_USER }}
#          key: ${{ secrets.SSH_PRIVATE_KEY }}
#          port: 22
#          script: |
#            cd /home/ubuntu/project
#            git pull origin master
#            source venv/bin/activate
#            pip install -r requirements.txt
#            python settings/manage.py makemigrations
#            python settings/manage.py migrate --noinput
#            python settings/manage.py collectstatic --noinput
#            sudo systemctl restart gunicorn


#name: Django CI/CD Pipeline with Docker
#
#on:
#  push:
#    branches: [ "master" ]
#
#env:
#  IMAGE_NAME: docker.io/${{ secrets.DOCKER_HUB_USERNAME }}/pytfastapimodels
#
#jobs:
#  build-and-push:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v4
#        with:
#          lfs: true
#
#      - name: Log in to Docker Hub
#        uses: docker/login-action@v3
#        with:
#          username: ${{ secrets.DOCKER_HUB_USERNAME }}
#          password: ${{ secrets.DOCKER_HUB_TOKEN }}
#
#      - name: Build and push Docker image
#        uses: docker/build-push-action@v5
#        with:
#          context: .
#          push: false
#          tags: ${{ env.IMAGE_NAME }}:latest
#
#  deploy:
#      runs-on: ubuntu-latest
#      needs: build-and-push
#      steps:
#        - name: Deploy to Server via SSH
#          uses: appleboy/ssh-action@v1.0.3
#          with:
#            host: ${{ secrets.SERVER_HOST }}
#            username: ${{ secrets.SERVER_USER }}
#            key: ${{ secrets.SSH_PRIVATE_KEY }}
#            port: 22
#            script: |
#              docker pull ${{ env.IMAGE_NAME }}:latest
#              docker stop docker-app || true
#              docker rm docker-app || true
#              docker run -d --name docker-app -p 8000:8000 ${{ env.IMAGE_NAME }}:latest